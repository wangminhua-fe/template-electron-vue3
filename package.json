{
  "name": "template-electron-vue3",
  "private": true,
  "version": "0.0.0",
  "main": "build/main.js",
  "scripts": {
    "dev": "vite",
    "dev:electron": "vite --config vite.config.electron.ts",
    "build": "vue-tsc && vite build",
    "build:electron": "vue-tsc && vite build --config vite.config.electron.ts && electron-builder",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.3.4"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.2.3",
    "electron": "^25.3.0",
    "electron-builder": "^24.4.0",
    "typescript": "^5.0.2",
    "vite": "^4.4.0",
    "vite-plugin-electron": "^0.12.0",
    "vue-tsc": "^1.8.3"
  }
}
